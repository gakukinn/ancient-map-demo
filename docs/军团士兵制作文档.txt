# 士兵视觉制作计划

## 1. 目标
解决士兵素材带绿色背景（Green Screen）的问题，并实现正确的8帧动画播放。
目标素材：S10DB 系列（如 484-1.png），特点是绿底、8个动作帧横向排列。

## 2. 技术方案

### 2.1 绿幕抠图 (Chroma Key)
**问题**：原始素材带有 `#00FF00` (或相近绿色) 的背景。
**解决**：在 `PhalanxDrawer` 预加载阶段进行像素处理。
- 使用 `OffscreenCanvas` 或临时 Canvas 加载图片。
- 获取 `ImageData`。
- 遍历所有像素，检测绿色分量。
  - 判定标准：`G > 200` 且 `R < 100` 且 `B < 100` (容差可调)。
  - 处理：将符合条件的像素 Alpha 值设为 0 (完全透明)。
- 将处理后的数据转回 `HTMLImageElement` 或直接存储为 `ImageBitmap` 供渲染使用。

### 2.2 动画切片 (Sprite Strip)
**问题**：素材是长条图，包含8个连续动作。
**解决**：
- **自动检测**：如果 `图片宽度 / 图片高度 > 4`，判定为动画序列帧。
- **帧数计算**：`帧数 = Math.floor(宽度 / 高度)` (通常为 8)。
- **渲染逻辑**：
  - 除了现有的 `direction` (8方向) 外，引入 `tick` (时间戳) 参数。
  - `当前帧 = (Math.floor(tick / 帧时长) % 总帧数)`。
  - 使用 `ctx.drawImage` 的源矩形参数 `sx, sy, sWidth, sHeight` 仅绘制当前帧。

## 3. 执行步骤
1.  **修改 `PhalanxDrawer.ts`**：
    - 重写 `preload` 方法，加入像素处理管线。
    - 增加 `processImage(img: HTMLImageElement): Promise<HTMLImageElement>` 辅助方法。
2.  **优化 `draw` 方法**：
    - 确保 `tick` 参数正确传递并用于计算帧索引。
    - 确保切片宽度计算正确。
3.  **测试**：
    - 运行游戏，检查玩家士兵背景是否变透明。
    - 检查玩家士兵是否在原地有动作循环。


## 4. 阵型与可视化规则 (Formation & Visualization) [New]

### 4.1 兵力可视化逻辑
*   **计算公式**:
    *   `visibleCount = floor(troops / 500)` (每 500 兵力对应 1 个可视化单位)
    *   **补兵逻辑**: 兵力恢复时，可视化单位会**瞬间**填满，不再逐帧增加。确保满兵状态下第一下受到伤害（如 20->19）能立即触发阵亡动画。
    *   上限: 100个可视化单位 (50000兵)
    *   下限: 1个可视化单位

### 4.2 扩展规则 (Formation Expansion) [Optimized]
*   **原则**: **完美矩形 (Perfect Rectangles)**。
*   **逻辑**: 兵力数字不再简单除以500，而是**向下取整**匹配到以下数列。确保方阵永远没有缺角，视觉极度舒适。

### 4.2.1 阵型阶梯表 (Formation Tiers) [Custom]
*   **原则**: 严格按照自定义阶梯显示，不再使用通用公式。
*   **逻辑**: 查找兵力满足的最高阶梯。

| 兵力 (Troops) | 显示人数 (Vis) | 阵型形状 (RxC) | 备注 |
| :--- | :--- | :--- | :--- |
| **50,000+** | **100** | 10x10 | 满编 (1万军团对应20单位，5万对应100) |
| 45,000+ | **90** | 9x10 | |
| 40,000+ | **81** | 9x9 | |
| 35,000+ | **72** | 8x9 | |
| 30,000+ | **64** | 8x8 | |
| 25,000+ | **56** | 7x8 | |
| 20,000+ | **49** | 7x7 | |
| 15,000+ | **42** | 6x7 | |
| 10,000+ | **36** | 6x6 | 常规军团 (36人) |
| 9,000+ | **30** | 5x6 | |
| 8,000+ | **25** | 5x5 | |
| 7,000+ | **20** | 4x5 | User: "5x4" (interpreted as 20 count) |
| 6,000+ | **16** | 4x4 | |
| 5,000+ | **12** | 3x4 | User: "4x3" (interpreted as 12 count) |
| 4,000+ | **9** | 3x3 | |
| 3,000+ | **6** | 2x3 | User: "3x2" (interpreted as 6 count) |
| 2,000+ | **4** | 2x2 | |
| 1,000+ | **3** | 1x3 | User: "3个兵" (Special: 1x3) |
| 500+ | **2** | 1x2 | User: "2个兵" |
| < 500 | **1** | 1x1 | User: "1个兵" |

*   **注**: 形状表示为 `Rows x Cols` (行x列)，通常保持 `Cols >= Rows` (宽阵) 以符合视觉习惯。

### 4.3 锚点与方向 (Anchor & Direction)
*   **锚点 (Pivot/Anchor)**: 阵型在此位置对齐屏幕中心坐标。
*   **旗帜位置**: 始终位于锚点位置。
*   **甩尾逻辑**: 锚点始终位于阵型行进方向的**最前端**。
    *   **朝左 (Visual Left)**: 锚点在 左列 + 中间行 (Left Center)。
    *   **朝右 (Visual Right)**: 锚点在 右列 + 中间行 (Right Center)。
    *   **朝上 (Visual Up)**: 锚点在 上行 + 中间列 (Top Center)。
    *   **朝下 (Visual Down)**: 锚点在 下行 + 中间列 (Bottom Center)。
    *   **NW/NE/SW/SE**: 锚点在 对应角 (Corner)。

*   **方向修正 (Direction Fix)**:
    *   发现代码中的方向判定与视觉存在偏差 (Visual Left 对应 Code NE 5)。
    *   已在 PhalanxDrawer 中建立映射表进行强制修正，确保所有方向视觉正确。

### 4.3.1 前排定义 (Frontline Definition) [Confirmed]
*   **核心逻辑**: **离锚点 (Anchor) 最近的士兵即为前排**。
    *   **一字长蛇 (正方向)**: 当朝向为 N/S/W/E 时，前排是与锚点同 行/列 的一整排士兵。
    *   **L型折角 (对角方向)**: 当朝向为 NW/NE/SW/SE 时，前排是锚点所在的 Row=0 和 Col=0 (或对应边界) 组成的两条边，呈“L型”或“倒L型”。
*   **应用**: 前排士兵负责肉搏、受击反馈和激烈的战斗动画，后排士兵负责阵型维持和支援动画。

### 4.4 主将 (General)
*   **位置**: 始终位于阵型的**几何中心** (`rows/2`, `cols/2`)。
*   **渲染顺序**: 先画士兵，后画主将 (确保主将在最上层不被遮挡)。
*   **素材**: 使用独立的主将素材 (IDLE/MOVE/ATTACK/DAMAGE)。

### 4.5 死亡表现
*   **逻辑**: 兵力减少导致 `visibleCount` 下降时，触发阵亡。
    *   **坐标锁定**: 尸体渲染时会**锁定在世界坐标 (Lat/Lng)**，不随军团移动而移动。
    *   **清理机制**: 
        *   战斗中：尸体保留。
        *   战斗结束 (`isFighting = false`)：自动清理所有尸体。
        *   初始化防止闪烁：初始化时仅创建 `visibleCount` 个单位，防止多余单位瞬间死亡导致尸体“诈尸”。
*   **动画**: 取消专门的“受伤(DYING)”动画，直接播放“阵亡(DEAD)”动画然后消失。


## 5. 玩家与旗帜素材规范 (Updated)

为了方便后续制作新素材，以下是已实现的素材规范：

### 5.1 玩家军团 (Phalanx System)
适用于玩家和标准军队，采用 5x5 方阵渲染。

*   **状态 (States)**:
    *   **移动 (Move)**: 8个方向，8帧/方向。
    *   **攻击 (Attack)**: 8个方向，8帧/方向。
    *   **待机 (Idle)**: 8个方向，循环播放。
    *   **受伤 (Damage)**: 8个方向，播放一次。
    *   **死亡 (Death)**: 8个方向，播放一次后定格。
*   **文件命名 (GameConfig 映射)**:
    *   目前使用 ID 映射数组 (如 `460-467` 对应移动的8个方向)。
    *   每个方向一张长条图 (Strip)。
    *   **尺寸**: 高度约 40-60px，宽度 = 高度 * 8 (帧数)。
    *   **朝向顺序**: 南(S) -> 东南(SE) -> 东(E) -> 东北(NE) -> 北(N) -> 西北(NW) -> 西(W) -> 西南(SW)。

### 5.2 旗帜系统 (Flag System)
由三个部分组成，自动组合渲染。

1.  **旗杆 (Pole)**
    *   **路径**: `/SUCAI/S10QZ/1-1.png`
    *   **说明**: 静态图片，会自动去绿底。
    *   **对齐**: 底部中心对齐军团中心。

2.  **旗面 (Flag Body)**
    *   **路径**: `/SUCAI/S10QZ/7-1.png` (蓝色旗帜)
    *   **格式**: 128x320 像素。
    *   **布局**: 4列 (帧) x 8行 (方向)。
    *   **尺寸**: 单帧 32x40。
    *   **朝向映射**: 0:NE, 1:E, 2:SE, 3:S, 4:SW, 5:W, 6:NW, 7:N。
    *   **逻辑**: 程序会根据朝向自动把旗面挂在旗杆的左侧或右侧。

3.  **文字 (Text Overlay)**
    *   **路径**: `/SUCAI/S10QZ/262-1.png` (白字 "帅")
    *   **格式**: 128x240 像素 (注意高度不同)。
    *   **布局**: 4列 (帧) x 6行 (有效文字行)。
    *   **特殊逻辑**:
        *   **第3行 (South)** 和 **第7行 (North)** 为空，程序会自动跳过不显示文字。
        *   其他方向一一映射。
    *   **对齐**: 严格对齐旗面坐标。

### 5.3 山贼样式 (Bandit Style) [Updated]
适用于所有**野外NPC单位**，使用更紧凑的 8x8 格式。
**适用类型**: `bandit` (盗贼), `raider` (流寇), `outlaw`, `barbarian` 等所有 NPC。

*   **格式**: 8x8 精灵表 (Sprite Sheet)。
    *   **布局**: 8行 (朝向 0-7) x 8列 (帧 0-7)。
    *   **尺寸**: 基础高度 60px * 缩放 2.0 (视觉匹配 5x5 方阵)。
*   **状态逻辑**:
    *   **移动 (Move)**: 循环播放 `MOVE` 文件的 0-7 帧。
    *   **待机 (Idle)**: 使用 `MOVE` 文件的第 0 帧。
        *   **逻辑**: 出生时**随机朝向** (0-7)，增加自然感。
    *   **攻击 (Attack)**:
        *   **文件**: 独立文件 (如 `91-1.png`)。
        *   **逻辑**: 自动**转向敌人** (Face Enemy)。
    *   **受伤 (Damage)**:
        *   **文件**: 独立文件 (如 `92-1.png`)。
        *   **逻辑**: 受到伤害时**闪烁 500毫秒**，随后恢复原状态。
    *   **死亡 (Death)**:
        *   **逻辑**: 直接消失，无额外动画。
*   **锚点 (Anchor)**:
    *   **对齐**: 脚下 (Bottom-Center)。
    *   **计算**: `center.y - renderHeight * 0.65` (视觉修正，确保红点在脚底)。

## 6. 方阵活力化系统 (Phalanx Vitality System) [New]
为解决方阵“僵硬”问题，引入了以下微观动画系统（仅应用于玩家和正规军团）：

### 6.1 错落动画 (Staggered Animation)
*   **问题**: 所有士兵动作完全同步（同时挥刀）。
*   **解决**: 基于士兵的 Grid 坐标生成随机种子，对动画帧索引 (`frameIndex`) 施加随机偏移。
*   **效果**: 即使是同一个兵种，不同位置的士兵动作也会有自然的时间差。

### 6.2 阵型波浪 (Gentle Wave)
*   **问题**: 士兵站位如钉死般固定。
*   **解决**: 在 `ATTACK` 状态下，引入低频的正弦波 (`Math.sin`) 偏移每个人物的 X/Y 坐标。
*   **弃用**: 曾尝试高频抖动 (Jitter)，因效果像帕金森而被移除。
*   **效果**: 模拟大军如麦浪般轻微起伏。

### 6.3 战斗呼吸 (Combat Breathing)
*   **问题**: 整体阵型是一个死板的矩形。
*   **解决**: 在 `ATTACK` 状态下，随时间调整 `spacingX` 和 `spacingY` (间距系数)。
*   **参数**: 周期约 2.5秒 (400 tick factor)，幅度 1% (0.01)。
*   **效果**: 整个方阵会随着战斗节奏有极微妙的“舒张”感。


### 6.4 动态前排 (Dynamic Frontline)
*   **逻辑**: 严格区分前排与后排。
    *   **前排 (Frontline)**: 执行 **攻击 (Attack)** 动作，承担战斗表现。
    *   **后排 (Backline)**: 执行 **待命 (Idle)** 动作，保持阵型严整，不再随乱挥刀。
*   **目的**: 增强战术纪律感，视觉上突显“交锋线”。

## 7. 常见坑点与注意事项 (Critical Pitfalls) [IMPORTANT]

### 7.1 方向映射错误 (Direction Mapping Error)
**此错误已发生多次，请务必注意！**
代码中的 `direction` (0-7) 与视觉上的 Frontline 判定存在特殊的等轴侧映射关系，**绝对不是**简单的罗盘对应。

*   **错误认知**: "朝西(West)" -> Code 6 -> Check Left? **WRONG!**
*   **正确映射**:
    *   **Code 0 (Visual NE)**: 朝向右上。前排在 Top & Right。
    *   **Code 1 (Visual E)** : 朝向右方。前排在 Right (Col+1)。
    *   **Code 2 (Visual SE)**: 朝向右下。前排在 Bottom & Right。
    *   **Code 3 (Visual S)** : 朝向下方。前排在 Bottom (Row+1)。
    *   **Code 4 (Visual SW)**: 朝向左下。前排在 Bottom & Left。
    *   **Code 5 (Visual W)** : **朝向左方**。前排在 **Left (Col-1)**。
    *   **Code 6 (Visual NW)**: 朝向左上。前排在 Top & Left。
    *   **Code 7 (Visual N)** : 朝向上方。前排在 Top (Row-1)。

**判断逻辑**: 必须使用 `PhalanxState.ts` 中的 `Visual Mapping` 表，严禁使用标准几何推导。

### 7.2 对象区分 (Entity Distinction: Player vs Legion) [CRITICAL]
**严禁混淆玩家与NPC军团！**
游戏中有两类独立的战斗单位，它们共享部分渲染逻辑，但配置完全独立：

1.  **玩家 (Player)**:
    *   **渲染器**: `LegionRenderer.ts`
    *   **贴图**: `SPRITE_PATHS.PHALANX` (Player Set)
    *   **尺寸**: **100** (Standard)
    *   **控制**: 玩家直接控制。

2.  **正规军团 (AI Legion)**:
    *   **渲染器**: `GlobalUnitRenderer.ts`
    *   **贴图**: `SPRITE_PATHS.AI_PHALANX` (Legion Set)
    *   **尺寸**: **50** (Small)
    *   **控制**: AI 逻辑控制。

**修改原则**: 修改其中一个时，**必须**检查是否意外影响了另一个。二者在 `PhalanxDrawer.ts` 中通过 `variant` 参数 ('player' | 'legion') 严格区分。

## 8. 技术实现总结 (Technical Summary) [Success]

### 8.1 核心架构 (Core Architecture)
*   **状态分离 (State Separation)**:
    *   **旧逻辑**: 渲染与逻辑混杂在 `PhalanxDrawer` 中，导致状态难以追踪。
    *   **新方案**: 引入 `PhalanxState.ts` 独立管理每个军团的 `UnitState` (Rows, Cols, Strings, States)。`PhalanxDrawer` 退化为纯渲染器。
*   **战斗锁定 (Combat Locking)**:
    *   进入战斗瞬间，记录当前的 `maxRows`, `maxCols` 和 `maxTroops` (作为 100%均值)。
    *   战斗中禁止阵型重排 (Resizing)，确保死尸位置固定，阵型只有减少没有变形。

### 8.2 关键算法解决方案 (Key Solutions)

#### A. 瞬死问题 (Instant Wipe-out)
*   **现象**: 兵力减少少量 (如 50人)，导致视觉上所有小人 (如 100个) 全部阵亡。
*   **原因**: 错误地将“兵力数值损失”直接映射为“视觉单位数量损失”。(50 兵力 > 100 视觉单位? No, but logic was flawed).
*   **修复**: **比例映射法 (Ratio Mapping)**。
    *   公式: `TargetAliveCount = ceil(TotalSlots * (CurrentTroops / InitialMaxTroops))`。
    *   效果: 兵力损失 10%，视觉单位也只倒下 10%。

#### B. 前排识别错误 (Wrong Frontline)
*   **现象**: 敌人从西边 (左侧) 进攻，甚至背后，前排士兵却在错误的位置挥刀。
*   **原因**: 混淆了 **Code Direction (0-7)** 与 **Screen Visual Direction**。在等轴侧视角下，Code 5 (通常为 NE) 实际上对应屏幕的“左方 (West)”。
*   **修复**: **视觉射线检测 (Visual Raycast)**。
    *   建立严格的映射表 (详见 7.1)。
    *   只判定“视觉上的前方邻居”是否存活。若前方邻居已死或出界，则我就是前排。

### 8.3 视觉优化
*   **随机倒下 (Random Death)**: 士兵阵亡时不再统一朝向，而是随机 `0-7` 方向倒下，增加战场的混乱与真实感。
*   **非战斗呼吸 (Idle Breathing)**: 仅在战斗且为前排时播放攻击动画，后排待机移动，层次感极强。

