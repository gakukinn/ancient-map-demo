# AI 军队逻辑设定

## 设计前提
- 每个阵营只有**一支军队**

## 核心规则 (3 条) — 防守优先

### 1. 己方城市被攻击 → 回防/增援
```
determineTarget (确定目标):
优先救援：检查 SiegeManager，找最近的被围己方城市。
其次进攻：找最近的敌方城市。
tickArmy (每帧执行):
如果正在战斗 → 什么都不做
如果正在移动 → 继续走
如果空闲 → 确定目标 → 开始移动

[升级] 动态增援机制：
1. 军队抵达正在交战的敌军/友军附近时，不再排队等待。
2. 自动判定阵营：若属于参战方，则直接作为“增援”加入战场。
3. 战力实时刷新：援军加入后会立即提升所属方总战力，改变损耗比例。
```

### 2. 无城市告急时 → 攻打最近敌城
```


### 3. 行军途中碰到敌军 → 自动野战
ContactEngine 检测相邻敌军，自动停止并开战。

---

## 状态机
      │    IDLE     │ ← 战斗结束后/初始状态
      └──────┬──────┘
             │ 有目标?
             ▼
      ┌─────────────┐
      │   MOVING    │ ← 朝目标城市移动
      └──────┬──────┘
             │ 碰到敌军 / 到达敌城
             ▼
      ┌─────────────┐
      │  FIGHTING   │ ← 战斗中 (野战或攻城)
      └──────┬──────┘
             │ 战斗结束
             ▼
           IDLE
```

---

## 行为树结构

```
RootSelector
├── IsInCombat           // 战斗中等待
├── DefendSequence       // 🛡️ 优先级1: 防守
│   ├── FindCityUnderSiege
│   └── MoveToTarget
├── AttackSequence       // ⚔️ 优先级2: 进攻
│   ├── FindEnemyCity
│   └── MoveToTarget
└── Idle
```
---

## [新增] 战斗规则 — 势力 1v1

### 野战
- **军队 vs 军队**
- 无第三方参与

### 攻城战  
- **进攻军队 vs 城市**
- **合并防御 (Merged Defense)**: 若城市周围 1 格内有己方军团，该军团会自动加入防御方，与其分摊伤害。
- **限制**: 单一势力只能有一支军队（Main Legion），必须由首都或指定据点出兵。已有存活军队时无法再次出兵。

### 4. 军队视觉表现 (S10 vs S8)
系统目前支持两套并行的军团渲染方案：

| 方案 | 特点 | 适用兵种示例 |
| :--- | :--- | :--- |
| **S10 (通用系)** | 8方向独立动作，以 3x3 阵型呈现。**支持完整的阵亡动画**（倒地过程）。<br> **指挥官特性**: 阵型（3x3/2-3-2）正中心的“主将”单位会放大至 **1.2倍** 显示，以突出指挥地位。 | 华夏步兵、骑兵、回回弓骑等。 |
| **S8 (精锐系)** | 动态拼接。移动 1 个大单体，战斗/等待 5 人一字横排。**不支持阵亡动画**（生命归零即消失）。 | 禁卫军团 (Xiyang)、各边疆军团等。 |



## 二，行军速度系统

### 1. 基础速度
所有计算基于一个全局基准速度常量 `STANDARD_SPEED = 0.1`。

### 2. 计算公式
最终行军速度 = `基准速度` * `道野倍率` * `兵种倍率` * `地形倍率`

### 3. 具体数值

#### 道野倍率
| 类型 | 倍率 | 说明 |
| :--- | :--- | :--- |
| **道路 (Road)** | **1.5x** | 优先级最高，覆盖下层地形。 |
| **野外 (Off-Road)** | **0.2x** | 仅限平原/浅滩等允许通行区域。**若无道路连接，AI禁止通过野外直连移动。** |

| **海洋 (Ocean)** | `OCEAN` | **0.0x** | **深海，不可通行 (Impassable)。** |

骑兵：平原快 (2x)，水路慢 (1.5x)。
步兵：水路快 (2x)，平原慢 (1.5x)。
山地：众生平等，地形 1.0x。
道路：统一提供 1.5x 加成。


## 三，战斗速度系统 (规模驱动)

### 1. 核心逻辑
使用 **规模系数 (Mass Factor)** 动态计算战斗时长，确保节奏既符合物理直觉（大兵团打得久），又严格控制在合理区间内。

### 2. 计算公式
```
战斗时长(秒) = 总兵力 / 1000
```
*结果限制在 [5.0秒, 45.0秒] 区间内。*

### 3. 时间基准
| 规模 | 兵力估算 | 时长 | 游戏时间 |
| :--- | :--- | :--- | :--- |
| **微型冲突** | < 5,000 人 | **5.0 秒** (保底) | 1/3 季度 |
| **标准野战** | 10,000 人 | **10.0 秒** | ~2/3 季度 |
| **大型战役** | 30,000 人 | **30.0 秒** | 2 季度 |
| **史诗决战** | 45,000 人+ | **45.0 秒** (封顶) | 3 季度 |



四，出兵系统。

### 1. 触发条件 (Trigger Condition)
- **单军团原则**: 每个阵营同一时间只能有一支主力军团在地图上活动。已有军团存活时，不会生成新军团。
- **兵力阈值**: 阵营所有城市的**可用兵力**（当前兵力 - 最低守军）总和必须大于 **20,000** 人。
  - *目的：防止兵力不足时频繁送死 (添油战术)。*
- **冷却时间**: 
  - 出兵成功：冷却 **60秒**。
  - 出兵失败（如无兵可调）：冷却 **30秒**。

### 2. 出兵兵力 (Troops)
- **数量设定**: 不设上限 (`Infinity`)。
- **实际执行**: 系统会征召阵营内所有城市的**盈余兵力**（保留最低守军后），有多少出多少。

### 3. 出兵地点 (Spawn Location)
- **优先级**: 
  1. 首都 (Capital)
  2. 皇都/巨城 (Imperial/Huge City)
  3. 大城 (Large City)
  4. 中城 (Medium City)
- **限制**: 只有上述类型的城市可以出兵。**关隘 (Pass) 和 小城 (Small City) 无法出兵**。

### 4. 目标选择 (Target Selection)
- **逻辑**: 寻找距离**最近**的敌方据点。
- **范围**: 包含所有**敌方城市、关隘 (Pass) 和 要塞 (Fortress)**。

五，战斗系统。

### 1. 核心机制
   
- **物理化时长**: 战斗时长由**总兵力规模**决定，而非瞬间结算。
  - 公式: $\text{Duration} = \text{TotalTroops} / 1000$
  - 范围: 5秒 ~ 45秒。


### 2. 战力计算

- **基础战力**: `兵力 (Troops)`。
- **运气成分**: 每次战斗开始时，双方（**无论是军队还是城市守军**）都会独立获得一个 **0.5x ~ 1.5x** 的随机战力倍率。
- **以少胜多**: 现在的方差非常大！理论上，运气极好的弱势方（1.5x）甚至可以战胜兵力是自己 **3倍** 但运气极差（0.5x）的对手。(1.5 / 0.5 = 3.0)
- **用范围**: 野战 (Army vs Army) 和 攻城战 (Army vs City) **通用**。守城方即使是静态单位，也拥有同样的“天命”判定。
- **关隘特权 (Pass Bonus)**: 
  - **防守修正**: 关隘 (Pass) 拥有 **0.5x** 的伤害减免系数（即受到的伤害只有正常的 50%）。
  - **战略意义**: 攻打关隘需要 2 倍于平常的兵力或时间。
- **基础战力**: 完全等于**兵力 (Troops)**。
- **适用范围**: 所有战斗通用。

### 3. 战斗结果
- **伤兵机制**: 获胜方在战斗结束后，立即恢复 **30%** 的阵亡兵力。
- **输家惩罚**: 
  - 野战：溃灭或撤退。
  - 攻城：防守方兵力归零，城市易主（重置为 1000 兵）。


本文档详细说明了当前版本（2026-01-24）的战斗系统设定与逻辑实现。

## 1. 核心设计理念

*   **独立实体**：军团（Legion/Army）与城市（City）是完全独立的实体。军团在地图上移动，城市固定不动。军团无法"进入"城市，只能驻扎在城市附近。
*   **物理化节奏**：战斗不是瞬间结算，而是基于"兵力规模"计算持续时间（5秒 - 20秒），模拟真实的交战过程。
*   **简化数值**：战斗力（Power）直接对应兵力（Troops），大兵团优势显著。
*   **1v1原则**：为了避免逻辑混乱，同一地块/同一单位在同一时间只能参与一场战斗。第三方试图介入时会被"阻塞"（Blocked）。

## 2. 触发机制 (Contact Engine)

战斗主要通过 `ContactEngine` 和 `SiegeManager` 触发。

### 2.1 野战 (Field Battle)
*   **触发条件**：当军团移动到敌对军团的**相邻六边形**（Adjacent Hex）时触发。
*   **判定逻辑**：
    1.  检测移动后周围 6 个格子的单位。
    2.  如果是敌对阵营 (Faction ID 不同)。
    3.  如果目标正处于战斗中 -> **动态增援** (检查是否可加入当前战场，若可加入则变为援军，否则阻塞)。
    4.  如果目标空闲 -> **触发野战**。
*   **结算后**：胜利方会有 2 秒的"冷却/僵直"时间，期间保持战斗状态，防止连续极其快速的接敌。

### 2.2 攻城战 (Siege Battle)
*   **触发条件**：
    1.  **碰撞触发**：军团试图移动到敌对城市所在的六边形（碰撞）。
    2.  **区域拦截 (ZOC)**：军团移动到敌对城市相邻格子时，会被城市拦截并强制进入攻城战。
    3.  **事件触发**：通过剧情/AI指令直接发起的攻城。
*   **判定逻辑**：
    *   如果城市是友方 -> 停止移动（驻驻）。
    *   如果城市正在被围攻 -> **动态增援** (若是相同阵营则加入攻/守方，若是第三方则阻塞)。
    *   否则 -> **触发攻城战**。

## 3. 战斗核心机制 (Combat System)

当前采用"时间驱动"（Time-Driven）的战斗模型。

### 3.1 时长计算 (Target Duration)
战斗持续时间并非固定，而是根据双方总兵力动态计算，以模拟大规模战役的胶着感。

$$
\text{主要公式: } \text{Duration} = \text{Base} + (\log_{10}(\text{TotalTroops}) \times \text{MassFactor})
$$

*   **基准时间 (Base)**: 5.0 秒
*   **规模系数 (Mass Factor)**: 2.5
*   **上下限**: 最短 5 秒，最长 30 秒。
*   **示例**:
    *   极小规模 (几百人): ~5 秒
    *   中等规模 (1万人): ~15 秒
    *   大规模 (10万人+): ~17.5 - 30 秒

### 3.2 伤害计算 (DPS Model)
系统预先计算"失败者需要多久死光"，以此反推每秒伤害（DPS）。

1.  **胜负预测**: 比较双方兵力（Troops）。兵力多者为预测胜者。
2.  **输家DPS (Loser DPS)**: $\frac{\text{输家当前兵力}}{\text{目标时长}}$
    *   保证输家在战斗结束时兵力归零。
3.  **赢家DPS (Winner DPS)**: $\text{输家DPS} \times \frac{\text{输家兵力}}{\text{赢家兵力}}$
    *   赢家受到的伤害与双方兵力比成反比。兵力优势越大，战损越小。

### 3.3 修正与保护
*   **保底机制**: 赢家兵力不会低于初始兵力的 10%（防止惨胜导致双方同归于尽）。
### 3.4 战斗结束
*   当任一方兵力低于 1.0 时，战斗结束。
*   **输家**: 兵力归零，状态设为 `isDestroyed`，从地图移除。

*   **例外**: 若赢家方有单位兵力归零（如守城军死光但援军赢了），该单位**免于销毁**，强制恢复至 **1000 兵力**（最低安全阈值），确保依然存活。

## 4. 兵种与军团 (Legions & Units)

### 4.1 兵种属性
目前的兵种差异主要体现在**视觉表现**上，数值上统一使用"兵力"计算。

配置 (`GameConfig.ts`) 定义了不同阵营的兵种外观：
*   **华夏系 (Huaxia)**: 步兵、骑兵、混合部队（步骑弩）。
*   **越南系 (Yuenan)**: 藤甲兵。
*   **回回系 (Huihui)**: 弓骑兵。


### 4.2 军团管理
*   **创建**: 军团只能通过事件或城市动员（Sortie）创建，不存在凭空刷新。
*   **动员**: 从城市征兵出战时，会扣除城市守军。
*   **移动**: 推荐使用道路移动 (`moveLegionToCity`)，系统会优先寻找道路路径。

## 5. 参数配置概览 (关键值)

| 参数项 | 当前值 | 说明 |
| :--- | :--- | :--- |
| **基础战斗时长** | 5.0s | 极小规模战斗时间 |
| **最大战斗时长** | 45.0s | 大规模战斗时间上限 (约4.5万人达到) |
| **军队移动速度** | 0.2 | 基础移动速度 (较慢) |
| **随机波动** | 0.5 | 战斗力波动范围 (0.5x ~ 1.5x) |
| **最小生存兵力** | 1.0 | 判定死亡的阈值 |
| **伤兵恢复率** | 10%-40% | 胜利后恢复已损失兵力的比例 |

当前的战斗系统是一个**重宏观、轻微操**的系统。它强调兵力规模对战斗节奏的影响，通过物理化的时长让战斗过程在地图上可视化，而不是瞬间跳出结果。这为"援军"赶战场提供了时间窗口，也强化了兵力优势的重要性。
